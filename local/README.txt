# GIT Commands

-- git

git add --all
git commit -am "0.03"
git push

git fetch --all
git reset --hard origin/master

git commit --amend -m "Replace commit message"

git tag -a 1.0.5 -m 'tag message'
git push --tags

